/**
 * Payment Method Identifiers (PMIs).
 *
 * These are used in CEP-8 `pmi` tags and in `notifications/payment_required`.
 */
export const PMI_BITCOIN_LIGHTNING_BOLT11 = 'bitcoin-lightning-bolt11' as const;
